#include "wclock.ceu"

{ Serial.begin(9600); }

loop do
    await 1s;
    var u32 us = _;
    emit WCLOCK_NOW(&us);
    {
        Serial.print("MS ");
        Serial.println(@us/1000);
    }
    emit WCLOCK_FREEZE(10000);
end
